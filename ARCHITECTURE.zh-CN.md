# 脱敏系统架构说明

本文用于展示公开 Demo 背后的正式系统设计，同时不公开真实数据、历史对比结果、账号密码或生产基础设施细节。

## 系统拆分

```text
公开作品集 Demo
GitHub Pages
    |
    +--> 合成当前数据
    +--> 合成上一轮基准
    +--> 筛选 / 排序 / 对比 UI

私有正式系统
定时采集任务
    |
    v
项目搜索抓取器
    |
    v
字段标准化 + 数据校验
    |
    +--> 当前 canonical dataset
    |
    +--> append-only change log
    |
    +--> 历史 snapshots
    |
    +--> 冻结轮次 baseline
    |
    v
对比 + 报告生成
    |
    v
受访问控制保护的正式 Viewer
```

## 正式数据流水线

### 1. 数据采集

定时任务获取学期交换项目列表与详情页。生产抓取逻辑与公开 Demo 分离，因为正式仓库会接触真实数据和运行状态。

### 2. 字段标准化

搜索页和详情页中的字段会转换为统一 schema，使申请状态、竞争状态、分数线、名额、资格条件等数据可以稳定地跨运行进行比较。

### 3. 数据校验与 Safety Guard

正式 tracker 在替换旧数据前会检查抓取结果。异常的大幅记录数下降、解析失败等情况会被视为不安全运行，而不是直接覆盖上一份有效数据。

### 4. 字段级变化检测

每次成功运行都会与上一份已知状态比较，并记录具体发生变化的字段，而不是只把整个项目粗略标记为“发生变化”。

### 5. 历史快照

历史 snapshot 与 append-only change records 用于审计和回溯，可以恢复项目数据随时间变化的过程。

### 6. 跨申请轮次比较

系统保留冻结的上一轮基准，与当前轮数据比较申请状态、竞争状态、分数线、名额和部分资格字段。

### 7. 私有报告输出

真实对比报告和正式 Viewer 属于私有生产输出，不提交到这个公开仓库。

## 公开 Demo 架构

公开 Demo 不包含 crawler 或后端。浏览器只读取两份完全合成的 JSON，并在前端执行筛选、排序和变化比较。

```text
current.json -----+
                  +--> 浏览器对比逻辑 --> 变化标签 / 表格 / 筛选器
comparison.json --+
```

这样招聘者可以直接检查交互和对比逻辑，同时公开仓库本身无法泄露生产历史数据。

## 隐私边界

公开仓库不包含：

- 生产数据集；
- 真实上一轮 baseline；
- 真实历史 snapshots 或 change logs；
- 自动生成的客户对比报告；
- 生产域名、主机、账号、密码或访问控制文件；
- 生产运行日志；
- 正式 crawler 源代码。

本仓库中的合成记录均为专门为 Demo 虚构的数据，不是对真实合作院校记录进行简单改名或变形得到的。

## 展示的工程能力

公开 Demo 与私有正式架构共同体现：

- 定时数据采集；
- schema 标准化；
- 防御式数据校验；
- 字段级 diff；
- append-only history 与 snapshots；
- 跨申请轮次比较；
- 报告生成；
- 访问控制部署；
- 双语搜索与多维筛选 UI；
- 公开源代码与私有生产数据的隔离设计。
